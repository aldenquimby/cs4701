uniform cost
	foreach (var m in sucessors(n))
	{
		if m is not OPEN or CLOSED
			add m to OPEN
		if m is OPEN
			if g(m) > g(n) + c(n,m)
				kill this node
	}



	if you've found two paths to the same state, the future is exactly the same
	and whichever one youre coming to second cannot be smaller than the existing one


	NOTE: if cost is always 1, uniform cost search = breadth first search